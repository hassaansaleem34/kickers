import 'package:flutter/material.dart';

class ComingSoon3 extends StatelessWidget {
  const ComingSoon3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,

          children: [Text("screen3")],
        ),
      ),
    );
  }
}
